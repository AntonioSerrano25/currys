import 'package:flutter/material.dart';

class perfiles_usuarios extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("PERFILES", style: TextStyle(fontSize: 30)),
    );
  }
}
